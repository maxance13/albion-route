{
  "name": "albion-route",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "mkdir -p public/guild && base64 -d guild-runtime.tar.gz.b64 | tar -xz -C . && node api/server.guild.js",
    "build": "node --check public/app.js && node scripts/build-static.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
